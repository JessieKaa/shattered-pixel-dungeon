-- demo_m58 entry script (M5b). Loaded by LuaEngine.loadModEntryScripts only when
-- demo_m58 is enabled (default_enabled=false). The per-type scripts under
-- scripts/{buffs,items,talents}/ load via loadXxxScripts regardless of entry,
-- so this file only emits a load banner. Toggle demo_m58 in the Mods UI (debug
-- builds) and restart to exercise the M5-M8 API surface end-to-end.
RPD.GLog("[demo_m58] loaded — M5-M8 API demo active")
